Currently put on hold as I realized using java swing (a deprecated GUI toolkit that's entirely been replaced by JavaFX) isn't the way forward.
On top of that, the program itself is very rough around the edges, I'd rather come back to it later and make it the best it can possibly be.
